enum AppStatus { initial, loading, success, error, empty }

enum SnackBarType { success, error, warning, info }

enum ButtonType { primary, secondary, outline, text }

enum ImageType { network, asset, svg, file }
